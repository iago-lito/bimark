
> make `install_github` command work!

* Get into JAGS and the deep part of bimark
    > add estimators and chain slots to the BimarkModel object
    > optionnaly choose which .JAGS model to use from `EstimateLatentCounts`
      arguments
    > better @example to GetBayesianModel()

> polish:
    > get through docs again
    > add examples to new functions

---

> Packaging:
    > protect `capture.events` and `nb.capture.events`?

